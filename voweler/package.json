{
  "name": "perceptron_viz",
  "version": "1.0.0",
  "description": "Simple frontend for training and visualizing a perceptron",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf dist && mkdir dist && cp public/* dist/",
    "build": "webpack --optimize-dedupe --devtool source-map",
    "postbuild": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Benson Tucker",
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.7.4",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.6.0",
    "c3": "^0.4.11-rc4",
    "jquery": "^2.2.2",
    "webpack": "^1.12.14"
  }
}
